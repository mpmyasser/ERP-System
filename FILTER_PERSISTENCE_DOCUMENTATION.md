# نظام حفظ التصفيات (Filter Persistence System)

## نظرة عامة
نظام تلقائي لحفظ واسترجاع اختيارات المستخدم من:
- **الأقسام** (Departments)
- **التواريخ** (Dates)
- **الحالات** (Status)
- **البحث** (Search)

## كيفية العمل

### 1. الحفظ التلقائي
- عند تغيير أي فلتر، يتم حفظه تلقائياً في **localStorage** المتصفح
- الحفظ يتم فور التغيير أو عند مغادرة الصفحة

### 2. الاسترجاع التلقائي
- عند فتح الصفحة مرة أخرى، يتم استرجاع الفلاترات المحفوظة تلقائياً
- الاختيارات تظهر كما كانت قبل آخر مرة زيارة

### 3. الحذف اليدوي
- يمكن حذف الفلاترات بالضغط على زر "إعادة تعيين" أو "Reset"

## الصفحات المدعومة

### 1. قائمة الموظفين (`/employees/`)
**الفلاترات المحفوظة:**
- البحث (Search)
- الأقسام (Departments)
- الحالة (Status: نشط/غير نشط)
- الوظيفة (Job Title)

**معرّفات العناصر (IDs):**
- `search` - حقل البحث
- `dept-filter` - قائمة الأقسام
- `status-filter` - قائمة الحالة
- `job-title-filter` - قائمة الوظيفة

### 2. الحضور اليومي (`/attendance/`)
**الفلاترات المحفوظة:**
- التاريخ (Date)
- الأقسام (Departments)

**معرّفات العناصر:**
- `date-picker` - حقل التاريخ
- `dept-filter` - قائمة الأقسام

### 3. القروض (`/loans/`)
**الفلاترات المحفوظة:**
- من تاريخ (Date From)
- إلى تاريخ (Date To)
- الأقسام (Departments)

**معرّفات العناصر:**
- `date-from` - تاريخ البداية
- `date-to` - تاريخ النهاية
- `dept-filter` - قائمة الأقسام

### 4. المكافآت (`/bonuses/`)
**الفلاترات المحفوظة:**
- من تاريخ (Date From)
- إلى تاريخ (Date To)
- الأقسام (Departments)

**معرّفات العناصر:**
- `date-from` - تاريخ البداية
- `date-to` - تاريخ النهاية
- `dept-filter` - قائمة الأقسام

### 5. العقوبات (`/penalties/`)
**الفلاترات المحفوظة:**
- من تاريخ (Date From)
- إلى تاريخ (Date To)
- الأقسام (Departments)

**معرّفات العناصر:**
- `date-from` - تاريخ البداية
- `date-to` - تاريخ النهاية
- `dept-filter` - قائمة الأقسام

### 6. الإجازات (`/permissions/`)
**الفلاترات المحفوظة:**
- من تاريخ (Date From)
- إلى تاريخ (Date To)
- الأقسام (Departments)

**معرّفات العناصر:**
- `date-from` - تاريخ البداية
- `date-to` - تاريخ النهاية
- `dept-filter` - قائمة الأقسام

## الملفات المرتبطة

### JavaScript
- **`app/static/js/filter_persistence.js`** - النظام الرئيسي

### Templates
جميع القوالب التي تم تحديثها بإضافة معرّفات العناصر:
- `app/templates/employees/list.html`
- `app/templates/attendance/daily.html`
- `app/templates/loans/list.html`
- `app/templates/bonuses/list.html`
- `app/templates/penalties/list.html`
- `app/templates/permissions/list.html`

## التخزين

### LocalStorage Keys
يتم تخزين الفلاترات في `localStorage` بمفاتيح مختلفة لكل صفحة:
- `employees_filters` - فلاترات الموظفين
- `attendance_filters` - فلاترات الحضور
- `loans_filters` - فلاترات القروض
- `bonuses_filters` - فلاترات المكافآت
- `penalties_filters` - فلاترات العقوبات
- `permissions_filters` - فلاترات الإجازات

### حجم التخزين
- **LocalStorage** لديها حد أقصى حوالي 5-10MB لكل نطاق
- البيانات محفوظة بصيغة JSON

## مثال على الاستخدام

### عندما يفتح المستخدم قائمة الموظفين:
1. يختار "قسم المبيعات" و "نشط" ويبحث عن "أحمد"
2. يُضغط على زر البحث
3. يتم حفظ الفلاترات تلقائياً في localStorage
4. عند العودة لاحقاً إلى الصفحة نفسها:
   - تظهر الأقسام والحالة والبحث كما كانت
   - جميع الموظفين بناءً على الفلاترات السابقة

## الدوال المتاحة

يمكن استدعاء الدوال يدويً إذا لزم الأمر:

```javascript
// حفظ الفلاترات يدويً
window.filterPersistence.save();

// استرجاع الفلاترات المحفوظة
window.filterPersistence.restore();

// حذف جميع الفلاترات المحفوظة للصفحة الحالية
window.filterPersistence.clear();

// الحصول على اسم الصفحة الحالية
window.filterPersistence.getCurrentPage();
```

## الملاحظات المهمة

1. **التخزين في المتصفح فقط**: البيانات تُحفظ محلياً في جهاز المستخدم، وليس على الخادم
2. **مشاركة الجهاز**: إذا شارك أكثر من شخص الجهاز/المتصفح، كل شخص له فلاترات منفصلة
3. **مسح البيانات**: عند مسح بيانات المتصفح (cache/cookies)، قد تُحذف الفلاترات
4. **الأمان**: البيانات تُخزن بشكل عام في localStorage، وليست مشفرة

## إضافة صفحة جديدة

لإضافة حفظ الفلاترات لصفحة جديدة:

1. أضف معرّفات (IDs) لجميع عناصر الفلاترات في القالب
2. أضف إدخال جديد في `FILTER_CONFIG` في `filter_persistence.js`:

```javascript
'new_page': {
    storageKey: 'new_page_filters',
    elements: {
        'element-id-1': 'filter_name_1',
        'element-id-2': 'filter_name_2'
    }
}
```

3. تأكد من أن اسم الصفحة يطابق اسم المسار الأول في URL (مثلاً `/loans/...` يُعتبر صفحة `loans`)
