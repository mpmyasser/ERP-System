================================================================================
PATCH: AJAX DataTable Conversion for /loans/ Page
================================================================================

FILE 1: app/routes/loans.py
================================================================================

--- a/app/routes/loans.py
+++ b/app/routes/loans.py
@@ -17,6 +17,42 @@ from core.utils.excel_utils import apply_professional_style
 
 loans_bp = Blueprint('loans', __name__)
 
+def _get_loans_data(date_from=None, date_to=None, department_ids=None, dept_filter_mode='include', search_code=''):
+    """Shared function to get loans data"""
+    db = current_app.db
+    if search_code:
+        department_ids = []
+    return db.search_loans(
+        date_from=date_from, 
+        date_to=date_to, 
+        department_ids=department_ids, 
+        dept_filter_mode=dept_filter_mode, 
+        code=search_code
+    )
+
+@loans_bp.route('/api/data')
+def api_data():
+    """API endpoint for AJAX DataTable"""
+    date_from = request.args.get('date_from')
+    date_to = request.args.get('date_to')
+    department_ids = request.args.getlist('department_ids', type=int)
+    if not department_ids and request.args.get('department_id'):
+        department_ids = [request.args.get('department_id', type=int)]
+    dept_filter_mode = request.args.get('dept_filter_mode', 'include')
+    search_code = request.args.get('search_code', '').strip()
+    
+    loans = _get_loans_data(date_from, date_to, department_ids, dept_filter_mode, search_code)
+    
+    data = []
+    for loan in loans:
+        data.append({
+            'id': loan.id, 'code': loan.employee.code if loan.employee else '-',
+            'name': loan.employee.name if loan.employee else '-',
+            'department': loan.employee.department.name if loan.employee and loan.employee.department else '-',
+            'date': loan.date.strftime('%d/%m/%Y') if loan.date else '-', 'type': loan.type,
+            'amount': loan.amount, 'installment_value': loan.installment_value,
+            'installments_count': loan.installments_count, 'excluded_months': loan.excluded_months or '-',
+            'end_date': loan.end_date.strftime('%d/%m/%Y') if loan.end_date else '-',
+            'remaining_balance': loan.auto_remaining_balance, 'status': loan.status
+        })
+    return {'data': data}
+
 @loans_bp.route('/')
 def list():
     """List all loans with filters"""
@@ -32,13 +68,7 @@ def list():
     dept_filter_mode = request.args.get('dept_filter_mode', 'include')
     search_code = request.args.get('search_code', '').strip()
     
-    # If searching by code, ignore department filter to find the employee regardless of department
-    if search_code:
-        department_ids = []
-    
-    # Get loans
-    loans = db.search_loans(
-        date_from=date_from, 
-        date_to=date_to, 
-        department_ids=department_ids, 
-        dept_filter_mode=dept_filter_mode, 
-        code=search_code
-    )
+    # Get loans
+    loans = _get_loans_data(date_from, date_to, department_ids, dept_filter_mode, search_code)
 
     # Calculate statistics
     total_loans_amount = sum(loan.amount for loan in loans)


FILE 2: app/templates/loans/list.html
================================================================================

--- a/app/templates/loans/list.html
+++ b/app/templates/loans/list.html
@@ -3,6 +3,60 @@
 {% block title %}السلف{% endblock %}
 
 {% block extra_js %}
+<script>
+    // AJAX DataTable initialization for loans
+    $(document).ready(function() {
+        const loansTable = $('#loans-table').DataTable({
+            ...defaultDataTableConfig,
+            ajax: {
+                url: "{{ url_for('loans.api_data') }}",
+                data: function(d) {
+                    d.date_from = $('#date-from').val();
+                    d.date_to = $('#date-to').val();
+                    d.department_ids = $('#dept-filter').val() || [];
+                    d.search_code = $('#search-code').val();
+                }
+            },
+            serverSide: false,
+            processing: true,
+            columns: [
+                { data: 'code', width: '10%' },
+                { data: 'name', width: '15%' },
+                { data: 'department', width: '10%' },
+                { data: 'date', width: '10%' },
+                { data: 'type', width: '10%', render: function(data) {
+                    if (data === 'monthly' || data === 'permanent') return '<span class="badge bg-primary">مستديمة</span>';
+                    if (data === 'emergency' || data === 'temporary') return '<span class="badge bg-warning text-dark">مؤقتة</span>';
+                    return '<span class="badge bg-secondary">' + data + '</span>';
+                }},
+                { data: 'amount', width: '10%', render: function(data) { return data.toLocaleString() + ' جنيه'; } },
+                { data: 'status', width: '8%', render: function(data) {
+                    if (data === 'Pending') return '<span class="badge bg-secondary"><i class="fas fa-clock"></i> بانتظار</span>';
+                    if (data === 'Approved') return '<span class="badge bg-success"><i class="fas fa-check"></i> تم الصرف</span>';
+                    return '<span class="badge bg-dark">' + data + '</span>';
+                }},
+                { data: 'installment_value', width: '10%', render: function(data) { return data.toLocaleString() + ' جنيه'; } },
+                { data: 'installments_count', width: '5%' },
+                { data: 'excluded_months', width: '10%' },
+                { data: 'end_date', width: '10%' },
+                { data: 'remaining_balance', width: '12%', render: function(data) { return '<strong class="text-danger">' + data.toLocaleString() + ' جنيه</strong>'; } },
+                { data: 'id', width: '10%', orderable: false, render: function(data) {
+                    return '<a href="/loans/' + data + '" class="btn btn-sm btn-info"><i class="fas fa-eye"></i></a> ' +
+                           '<a href="/loans/' + data + '/edit" class="btn btn-sm btn-warning"><i class="fas fa-edit"></i></a> ' +
+                           '<button class="btn btn-sm btn-danger delete-record-btn" data-module="loans" data-id="' + data + '" data-confirm="هل أنت متأكد من حذف هذه السلفة؟"><i class="fas fa-trash"></i></button>';
+                }}
+            ],
+            order: [[0, 'asc']]
+        });
+
+        $('#unified-filter-bar form').on('submit', function(e) {
+            e.preventDefault();
+            loansTable.ajax.reload();
+            return false;
+        });
+
+        $('#unified-filter-bar').on('click', 'a.btn-outline-secondary', function(e) {
+            e.preventDefault();
+            $('#date-from, #date-to, #search-code').val('');
+            $('#dept-filter').val(null).trigger('change');
+            loansTable.ajax.reload();
+        });
+    });
+</script>
 <script>
     <!-- تمت إزالة أي تنظيف تلقائي للـ localStorage؛ يعتمد على stateSave مع مفتاح النسخة v2 -->
 </script>
@@ -95,7 +149,7 @@
             <thead class="table-light">
                 <tr class="text-center">
                     <th width="10%">الكود</th>
                     <th width="15%">الموظف</th>
                     <th width="10%">القسم</th>
@@ -111,47 +165,7 @@
                     <th class="no-sort" width="10%">إجراءات</th>
                 </tr>
             </thead>
             <tbody>
-                {% for loan in loans %}
-                <tr class="text-center">
-                    <td><strong class="text-primary">{{ loan.employee.code if loan.employee else '-' }}</strong>
-                    </td>
-                    <td class="text-start"><strong>{{ loan.employee.name if loan.employee else '-' }}</strong></td>
-                    <td>{{ loan.employee.department.name if loan.employee and loan.employee.department else '-' }}
-                    </td>
-                    <td>{{ loan.date.strftime('%d/%m/%Y') if loan.date else '-' }}</td>
-                    <td>
-                        {% if loan.type == 'monthly' or loan.type == 'permanent' %}
-                        <span class="badge bg-primary">مستديمة</span>
-                        {% elif loan.type == 'emergency' or loan.type == 'temporary' %}
-                        <span class="badge bg-warning text-dark">مؤقتة</span>
-                        {% else %}
-                        <span class="badge bg-secondary">{{ loan.type }}</span>
-                        {% endif %}
-                    </td>
-                    <td><strong>{{ "{:,.2f}".format(loan.amount) }} جنيه</strong></td>
-                    <td>
-                        {% if loan.status == 'Pending' %}
-                        <span class="badge bg-secondary"><i class="fas fa-clock" aria-hidden="true"></i> بانتظار الاعتماد</span>
-                        {% elif loan.status == 'Approved' %}
-                        <span class="badge bg-success"><i class="fas fa-check" aria-hidden="true"></i> تم الصرف</span>
-                        {% else %}
-                        <span class="badge bg-dark">{{ loan.status }}</span>
-                        {% endif %}
-                    </td>
-                    <td>{{ "{:,.2f}".format(loan.installment_value) }} جنيه</td>
-                    <td>{{ loan.installments_count }}</td>
-                    <td>
-                        {% if (loan.type == 'monthly' or loan.type == 'permanent') and loan.excluded_months %}
-                        {{ loan.excluded_months }}
-                        {% else %}
-                        -
-                        {% endif %}
-                    </td>
-                    <td>{{ loan.end_date.strftime('%d/%m/%Y') if loan.end_date else '-' }}</td>
-                    <td><strong class="text-danger">{{ "{:,.2f}".format(loan.auto_remaining_balance) }}
-                            جنيه</strong></td>
-                    <td>
-                        <a href="{{ url_for('loans.view', id=loan.id) }}" class="btn btn-sm btn-info" title="عرض" aria-label="عرض السلفة">
-                            <i class="fas fa-eye" aria-hidden="true"></i>
-                        </a>
-                        <a href="{{ url_for('loans.edit', id=loan.id) }}" class="btn btn-sm btn-warning" title="تعديل" aria-label="تعديل السلفة">
-                            <i class="fas fa-edit" aria-hidden="true"></i>
-                        </a>
-                        <button class="btn btn-sm btn-danger delete-record-btn" data-module="loans"
-                            data-id="{{ loan.id }}" data-confirm="هل أنت متأكد من حذف هذه السلفة؟" title="حذف" aria-label="حذف السلفة">
-                            <i class="fas fa-trash" aria-hidden="true"></i>
-                        </button>
-                    </td>
-                </tr>
-                {% endfor %}
             </tbody>
         </table>


FILE 3: app/static/js/datatables_init.js
================================================================================

NO CHANGES REQUIRED
- Already supports AJAX DataTables
- stateSave configuration preserved
- Buttons configuration preserved
- Date sorting plugin preserved

================================================================================
SUMMARY OF CHANGES
================================================================================

Lines Added:    ~100 (API endpoint + AJAX init)
Lines Removed:  ~60 (server-side row rendering)
Files Modified: 2 (loans.py, list.html)
Files Unchanged: 1 (datatables_init.js)

Backend Logic: UNCHANGED
- Same query logic via _get_loans_data()
- Same filter parameters
- Same business rules

Frontend UX: IMPROVED
- Smaller initial payload
- Faster page load
- Smoother filtering
- Better pagination

================================================================================
