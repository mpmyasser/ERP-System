# Engineering Hypotheses

هذا الملف يحتوي فقط على ملاحظات **مشبوهة لكن غير مُثبَتة بعد** — أفكار أو أنماط
لوحظت أثناء التنفيذ أو التحقق، لكن الأدلة المتوفرة حاليًا غير كافية لتأكيدها
كحقيقة. لا يُنقل أي عنصر من هنا إلى `TECHNICAL_DEBT.md` إلا بعد التحقق المباشر
منه بدليل ملموس (كود، نتيجة تنفيذ فعلية، سجل أخطاء حقيقي... إلخ).

---

## HYP-001 — [محسومة ✅ نُقلت إلى TECHNICAL_DEBT.md كـ TD-005]

**تحديث 2026-07-22:** تم إثبات هذه الفرضية بالكامل بسكريبت تكرار مباشر ومعزول تمامًا (استعلام واحد فقط، بدون أي استعلام منافس). السبب الجذري: `Query.get()` (والمكافئ `Session.get()`) في SQLAlchemy 2.0 **يتجاوز خيارات `.options(joinedload(...))` بصمت** — وهذا ليس خاصًا بمشروعنا بل سلوك موثَّق لـSQLAlchemy نفسها. تفاصيل الإثبات والنمط الخطير الفعلي الموجود في كود الإنتاج الحالي (وليس افتراضيًا) موثَّقة في **TD-005** بـ`TECHNICAL_DEBT.md`.

---

## HYP-002 — Other `joinedload` Usages in the Codebase May Share the Same Session-Close-Before-Access Risk

| Field | Value |
|-------|-------|
| **Status** | Unverified (نطاق مُضيَّق بعد اكتشاف TD-005) |
| **Related to** | TD-003, TD-005 |

**الفرضية (مُحدَّثة):** بعد اكتشاف أن السبب الجذري الحقيقي هو تحديدًا تركيبة `.options(joinedload(...)).get(...)` (وليس أي استخدام لـ`joinedload` بشكل عام)، ضاق نطاق هذه الفرضية: با��تحديد، **استخدامات `joinedload` الأخرى التي تنتهي بـ`.all()` أو `.first()` مُثبَتة الآن كآمنة تمامًا** (تحققت مباشرة: `.filter_by().first()` يُحمِّل العلاقة بشكل صحيح ويبقى متاحًا بعد إغلاق الجلسة). الخطر الحقيقي المتبقي محصور فقط في مواضع `joinedload(...).get(...)` الأخرى غير المفحوصة بعد لجهة ترتيب "العرض قبل الإغلاق".

**الأدلة المتوفرة:**
- بحث نصي دقيق `grep "options(joinedload" | grep "\.get("` عبر كل `app/routes/` أظهر **3 مواضع فقط** (وليس عشرات كما كان مفترضًا سابقًا): `app/routes/treasury.py:399`، و`app/routes/accounting.py:136`، و`app/routes/accounting.py:161`.
- فحصت `treasury.py:399` (دالة `voucher_form` تحديدًا): `render_template()` يحدث قبل `db_session.close()` — **آمن حاليًا بنفس "الصدفة البنيوية"** كدالة `dashboard()`.

**الأدلة الناقصة:**
- لم أفحص `accounting.py:136` و`accounting.py:161` (دالة الإيصال / journal entry) لتحديد ترتيب العرض/الإغلاق فيهما.
- لم أُحدَّد ما إذا كانت `voucher_form.html` أو قالب `accounting` المعني يصل فعليًا لـ`selected_cash.account.X` أو `entry.items` (إن لم يصل القالب للعلاقة أصلًا، فالمخاطرة نظرية حتى لو انعكس ترتيب الكود مستقبلًا).

**لتحويل هذه الفرضية إلى `TECHNICAL_DEBT.md`:** يلزم فحص القالبين المرتبطين بـ`accounting.py:136/161` بنفس الطريقة المُتبعة مع `treasury.py`.

---

## سجل الإصدارات

| التاريخ | التغيير |
|---------|---------|
| 2026-07-22 | إنشاء الملف، إضافة HYP-001 وHYP-002 (مأخوذتين من تصحيح TD-003 بعد اكتشاف أن الاستنتاج الأصلي كان تفسيرًا غير مُثبَت) |
