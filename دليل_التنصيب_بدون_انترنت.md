# دليل التنصيب بدون إنترنت

لكي يعمل المشروع على جهاز جديد لا يحتوي على Python أو أي مكتبات، يجب نقل مجلد المشروع كاملًا، وليس مجلد `offline_packages` فقط.

يجب أن تحتوي الحزمة على:

- مجلد المشروع كاملًا.
- ملف `install_offline.bat`.
- ملف `start_hr.bat`.
- مجلد `offline_packages`.
- ملف `offline_packages\requirements.txt`.
- كل ملفات `.whl` داخل `offline_packages`.
- ملف `offline_packages\python-3.11.9-amd64.exe`.

سبب استخدام Python 3.11.9: الحزم الموجودة مبنية لإصدار `cp311-win_amd64`، أي Python 3.11 على Windows 64-bit. كما أن Python 3.11.9 هو آخر إصدار 3.11 يوفر مثبت Windows عادي.

## تجهيز الحزمة على جهاز متصل بالإنترنت

شغل:

```bat
python create_offline_installer.py
```

سيقوم السكربت بتنزيل مكتبات المشروع ومثبت Python داخل `offline_packages`.

## التنصيب على الجهاز غير المتصل بالإنترنت

1. انسخ مجلد المشروع كاملًا إلى الجهاز الجديد.
2. شغل `install_offline.bat`.
3. بعد نجاح التنصيب شغل `start_hr.bat`.

ملف التنصيب ينشئ بيئة محلية باسم `.venv` ويثبت المكتبات من الملفات المحلية فقط. عند حدوث خطأ ستجد التفاصيل في `offline_install.log`.
