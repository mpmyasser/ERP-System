# دليل التنصيب بدون انترنت

هذا الدليل يشرح كيفية تنصيب مكتبات المشروع بدون الحاجة للاتصال بالإنترنت.

## الخطوات

1.  **تجهيز ملفات التنصيب:**
    *   قم بتشغيل ملف `create_offline_installer.py`:
        ```bash
        python create_offline_installer.py
        ```
    *   سيقوم هذا الأمر بإنشاء مجلد جديد باسم `offline_packages` يحتوي على جميع المكتبات المطلوبة.
    *   سيقوم أيضًا بإنشاء ملف تنصيب باسم `install_offline.bat`.

2.  **نقل ملفات التنصيب:**
    *   قم بنسخ مجلد `offline_packages` وملف `install_offline.bat` إلى الجهاز الذي تريد تنصيب المكتبات عليه.

3.  **تنصيب المكتبات بدون انترنت:**
    *   على الجهاز الهدف، قم بتشغيل ملف `install_offline.bat`. سيقوم هذا بتنصيب جميع المكتبات من مجلد `offline_packages`.

الآن لديك كل ما تحتاجه لتنصيب المشروع بدون انترنت.
