# 📚 دليل نظام الإجازات المتقدم

## 🎯 نظرة عامة

تم إضافة نظام إجازات متقدم كامل مع:
- ✅ رصيد إجازات تلقائي لكل موظف
- ✅ أنواع إجازات متعددة (سنوية، مرضية، عارضة، طارئة، بدون راتب)
- ✅ إدخال جماعي (Bulk Entry) مثل السلف
- ✅ متابعة وتتبع الأرصدة
- ✅ حساب تلقائي في الرواتب

---

## 📊 أنواع الإجازات والأرصدة الافتراضية

| النوع | الرصيد السنوي | بأجر؟ | الوصف |
|-------|---------------|-------|-------|
| **سنوية** | 21 يوم | ✅ نعم | الإجازة السنوية الاعتيادية |
| **مرضية** | 30 يوم | ✅ نعم | الإجازات المرضية بموجب شهادة |
| **عارضة** | 7 أيام | ✅ نعم | إجازات قصيرة لظروف طارئة |
| **طارئة** | 3 أيام | ✅ نعم | إجازات عاجلة |
| **بدون راتب** | غير محدود | ❌ لا | إجازات بدون راتب |

---

## 🚀 دليل الاستخدام السريع

### **1. تهيئة الأرصدة (خطوة واحدة فقط)**

قبل استخدام النظام، يجب تهيئة أرصدة جميع الموظفين:

1. اذهب إلى: **الإجازات** → **الأرصدة**
2. اختر السنة المالية
3. اضغط **تهيئة الأرصدة**
4. سيتم إنشاء رصيد تلقائي لكل موظف

---

### **2. إدخال إجازات جماعي (Bulk Entry)**

الطريقة الأسرع لإدخال إجازات متعددة:

#### **الخطوات:**

1. اذهب إلى: **الإجازات** → **إدخال جماعي**

2. **إعدادات الدفعة** (افتراضيات):
   - **تاريخ البداية**: يُملأ تلقائياً للصفوف الجديدة
   - **تاريخ النهاية**: يُملأ تلقائياً
   - **نوع الإجازة**: الافتراضي
   - **عدد الأيام**: الافتراضي

3. **إدخال البيانات**:
   - **كود الموظف**: ابدأ الكتابة وسيظهر اقتراحات تلقائية
   - **الاسم**: يُملأ تلقائياً عند إدخال الكود
   - **نوع الإجازة**: اختر من القائمة
   - **من تاريخ / إلى تاريخ**: DD/MM/YYYY
   - **عدد الأيام**: يُحسب تلقائياً أو أدخله يدوياً
   - **السبب**: اختياري

4. **إضافة صفوف**:
   - اضغط **إضافة صف جديد** لكل موظف إضافي

5. **الحفظ**:
   - اضغط **حفظ الكل**
   - يتم تحديث الأرصدة تلقائياً

#### **مثال:**
```
كود: 001 | الاسم: أحمد محمد | نوع: سنوية | من: 01/01/2025 | إلى: 05/01/2025 | الأيام: 5
كود: 002 | الاسم: محمود علي | نوع: مرضية | من: 10/01/2025 | إلى: 12/01/2025 | الأيام: 3
```

---

### **3. عرض قائمة الإجازات**

**الوصول:** الإجازات → قائمة الإجازات

**الفلاتر المتاحة:**
- من تاريخ / إلى تاريخ
- نوع الإجازة
- القسم

**الإحصائيات:**
- إجمالي الإجازات
- إجمالي الأيام
- عدد الموظفين

**الخيارات:**
- 🗑️ حذف إجازة (يسترجع الرصيد تلقائياً)

---

### **4. متابعة الأرصدة**

**الوصول:** الإجازات → الأرصدة

**المعلومات المعروضة:**
- الرصيد المستخدم / المتبقي لكل نوع
- مؤشرات لونية (أخضر = جيد، أصفر = تحذير، أحمر = قليل)

**الفلاتر:**
- السنة المالية
- القسم

---

## 🔧 الملفات المضافة

### **Backend:**
```
core/database_models.py          # 3 نماذج جديدة: LeaveTypeEnum, LeaveBalance, Leave
core/services/leave_service.py   # خدمة إدارة الإجازات
app/routes/leaves.py              # Routes: list, bulk, balances, delete
```

### **Frontend:**
```
app/templates/leaves/bulk.html      # إدخال جماعي
app/templates/leaves/list.html      # قائمة الإجازات
app/templates/leaves/balances.html  # عرض الأرصدة
```

### **Database:**
```sql
CREATE TABLE leave_balances (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER,
    year INTEGER,
    annual_balance FLOAT DEFAULT 21.0,
    annual_used FLOAT DEFAULT 0.0,
    sick_balance FLOAT DEFAULT 30.0,
    sick_used FLOAT DEFAULT 0.0,
    casual_balance FLOAT DEFAULT 7.0,
    casual_used FLOAT DEFAULT 0.0,
    emergency_balance FLOAT DEFAULT 3.0,
    emergency_used FLOAT DEFAULT 0.0
);

CREATE TABLE leaves (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER,
    leave_type VARCHAR,
    start_date DATE,
    end_date DATE,
    days_count FLOAT,
    is_paid BOOLEAN DEFAULT TRUE,
    status VARCHAR DEFAULT 'موافق عليها',
    reason TEXT,
    requested_date DATE
);
```

---

## 💡 نصائح وأفضل الممارسات

### **1. تهيئة الأرصدة**
- نفذها **مرة واحدة** في بداية كل سنة مالية
- يمكن إعادة التنفيذ للموظفين الجدد

### **2. الإدخال الجماعي**
- استخدمه عند تسجيل إجازات متعددة في وقت واحد
- يدعم الإكمال التلقائي لأكواد الموظفين
- يحسب الأيام تلقائياً من التواريخ

### **3. إدارة الأرصدة**
- الأرصدة تُحدث تلقائياً عند إدخال/حذف إجازات
- راقب المؤشرات اللونية لتنبيه الموظفين
- الإجازات بدون راتب لا تؤثر على الأرصدة

### **4. التكامل مع الرواتب**
- الإجازات بأجر: لا تؤثر على الراتب
- الإجازات بدون راتب: تُخصم من الراتب تلقائياً
- يتم الحساب ضمن نظام الرواتب الشهري

---

## 🔗 الروابط السريعة

| الصفحة | المسار |
|--------|--------|
| قائمة الإجازات | `/leaves/` |
| إدخال جماعي | `/leaves/bulk` |
| الأرصدة | `/leaves/balances` |
| إجازات موظف معين | `/leaves/employee/<emp_id>` |

---

## ⚙️ التخصيص

### **تعديل الأرصدة الافتراضية:**

عدّل في `core/services/leave_service.py`:

```python
balance = LeaveBalance(
    employee_id=employee_id,
    year=year,
    annual_balance=21.0,   # ← غيّر هنا
    sick_balance=30.0,     # ← غيّر هنا
    casual_balance=7.0,    # ← غيّر هنا
    emergency_balance=3.0  # ← غيّر هنا
)
```

### **إضافة أنواع إجازات جديدة:**

عدّل في `core/database_models.py`:

```python
class LeaveTypeEnum(enum.Enum):
    ANNUAL = "سنوية"
    SICK = "مرضية"
    CASUAL = "عارضة"
    UNPAID = "بدون راتب"
    EMERGENCY = "طارئة"
    MATERNITY = "وضع"
    BEREAVEMENT = "وفاة"
    HAJJ = "حج"  # ← مثال: أضف هنا
```

---

## 🐛 استكشاف الأخطاء

### **المشكلة: "لا توجد أرصدة مسجلة"**
**الحل:** اذهب إلى الأرصدة واضغط "تهيئة الأرصدة"

### **المشكلة: "الموظف غير موجود"**
**الحل:** تأكد من كتابة كود الموظف بشكل صحيح

### **المشكلة: "الرصيد غير كافٍ"**
**الحل:** تحقق من رصيد الموظف في صفحة الأرصدة

---

## ✅ قائمة المهام المكتملة

- [x] إنشاء جداول قاعدة البيانات
- [x] نماذج LeaveBalance و Leave
- [x] خدمة LeaveService
- [x] Routes: list, bulk, balances, delete
- [x] واجهة إدخال جماعي (Bulk Entry)
- [x] واجهة قائمة الإجازات
- [x] واجهة الأرصدة
- [x] تكامل مع القائمة الرئيسية
- [x] حساب تلقائي للأرصدة
- [x] استرجاع الرصيد عند الحذف
- [x] فلاتر متقدمة
- [x] إحصائيات

---

## 📞 الدعم

للمساعدة أو التعديلات الإضافية، راجع:
- الكود في `app/routes/leaves.py`
- الخدمة في `core/services/leave_service.py`
- النماذج في `core/database_models.py`

---

**تم بنجاح! 🎉**
