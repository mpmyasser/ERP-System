# دليل اختبار تحديثات نموذج الموظف
# Testing Guide for Employee Form Updates

## 📋 الاختبارات المتاحة

### 1. اختبار منطق التحقق من الحقول

**تشغيل الاختبار:**
```bash
python test_employee_form.py
```

**النتيجة المتوقعة:**
```
✅ اختبار 1: التحقق من الرقم القومي
   ✅ 14 رقم - صحيح
   ✅ 13 رقم - خاطئ
   ✅ 15 رقم - خاطئ
   ✅ يحتوي على أحرف - خاطئ
   ✅ فارغ - مقبول (اختياري)

✅ اختبار 2: التحقق من رقم الموبايل
   ✅ 11 رقم - صحيح
   ✅ 10 أرقام - صحيح
   ✅ 12 رقم - خاطئ
   ✅ 8 أرقام - خاطئ
   ✅ يحتوي على أحرف - خاطئ
   ✅ فارغ - مقبول (اختياري)
```

---

## 🌐 اختبار واجهة الويب

### 2. تشغيل الخادم

```bash
python run.py
```

أو استخدام البرنامج النصي:
```bash
start_hr.bat
```

---

### 3. اختبار إضافة موظف جديد

**الخطوات:**
1. اذهب إلى `http://localhost:5000/employees`
2. انقر على زر "إضافة موظف جديد"
3. أملأ النموذج حسب الحالات التالية:

#### الحالة 1: موظف بدون رقم قومي أو موبايل

**الإدخال:**
```
الاسم: أحمد محمد
الكود: EMP001
الرقم القومي: (اترك فارغاً)
الموبايل: (اترك فارغاً)
تاريخ التعيين: 01/01/2024
```

**النتيجة المتوقعة:**
```
✅ تم إضافة الموظف أحمد محمد بنجاح
```

---

#### الحالة 2: موظف برقم قومي صحيح وموبايل خاطئ

**الإدخال:**
```
الاسم: فاطمة علي
الكود: EMP002
الرقم القومي: 12345678901234 (14 رقم ✅)
الموبايل: 123456 (قصير ❌)
تاريخ التعيين: 01/01/2024
```

**النتيجة المتوقعة:**
```
✅ تم إضافة الموظف فاطمة علي بنجاح
⚠️ رقم الموبايل يجب أن يكون 10-11 رقم
```

---

#### الحالة 3: موظف برقم قومي خاطئ وموبايل صحيح

**الإدخال:**
```
الاسم: سارة حسن
الكود: EMP003
الرقم القومي: 123456 (قصير ❌)
الموبايل: 01012345678 (11 رقم ✅)
تاريخ التعيين: 01/01/2024
```

**النتيجة المتوقعة:**
```
✅ تم إضافة الموظف سارة حسن بنجاح
⚠️ الرقم القومي يجب أن يكون 14 رقم
```

---

#### الحالة 4: موظف برقم قومي وموبايل خاطئ

**الإدخال:**
```
الاسم: محمود عبدالرحمن
الكود: EMP004
الرقم القومي: ABC (أحرف ❌)
الموبايل: 123 (قصير ❌)
تاريخ التعيين: 01/01/2024
```

**النتيجة المتوقعة:**
```
✅ تم إضافة الموظف محمود عبدالرحمن بنجاح
⚠️ الرقم القومي يجب أن يكون 14 رقم
⚠️ رقم الموبايل يجب أن يكون 10-11 رقم
```

---

#### الحالة 5: موظف بدون اسم (يجب أن يفشل)

**الإدخال:**
```
الاسم: (اترك فارغاً) ❌
الكود: EMP005
الرقم القومي: 12345678901234
تاريخ التعيين: 01/01/2024
```

**النتيجة المتوقعة:**
```
❌ يتم عرض النموذج مرة أخرى
❌ يظهر رسالة الخطأ: "هذا الحقل مطلوب" تحت حقل الاسم
```

---

### 4. اختبار تحديث بيانات الموظف

**الخطوات:**
1. اذهب إلى قائمة الموظفين
2. انقر على موظف موجود للعرض
3. انقر على زر "تعديل"
4. عدّل البيانات حسب الحالات السابقة

**النتيجة المتوقعة:** نفس السلوك كما في الإضافة

---

## 🔍 التحقق من قاعدة البيانات

### 5. التحقق من البيانات المحفوظة

**لفتح قاعدة البيانات:**
```bash
python
>>> from core.db_manager import DBManager
>>> db = DBManager()
>>> emp = db.get_employee_by_code('EMP001')
>>> print(f"الاسم: {emp.name}")
>>> print(f"الرقم القومي: {emp.national_id}")
>>> print(f"الموبايل: {emp.mobile_number}")
```

**النتيجة المتوقعة:**
```
الاسم: أحمد محمد
الرقم القومي: None
الموبايل: None
```

---

## 📊 ملخص حالات الاختبار

| الحالة | الاسم | الكود | الرقم القومي | الموبايل | النتيجة |
|-------|-------|-------|------------|---------|--------|
| 1 | ✅ | ✅ | (فارغ) | (فارغ) | ✅ حفظ |
| 2 | ✅ | ✅ | ✅ صحيح | ❌ خاطئ | ✅ حفظ + تحذير |
| 3 | ✅ | ✅ | ❌ خاطئ | ✅ صحيح | ✅ حفظ + تحذير |
| 4 | ✅ | ✅ | ❌ خاطئ | ❌ خاطئ | ✅ حفظ + تحذيرين |
| 5 | ❌ فارغ | ✅ | ✅ | ✅ | ❌ رفض |
| 6 | ✅ | ❌ فارغ | ✅ | ✅ | ❌ رفض |

---

## 🐛 استكشاف الأخطاء

إذا واجهت مشاكل:

### مشكلة: لا تظهر التحذيرات
**الحل:**
```bash
# تحقق من أن base.html تدعم الرسائل متعددة الأسطر
grep -n "split" app/templates/base.html
```

### مشكلة: يتم حفظ البيانات الخاطئة
**الحل:**
```bash
# تحقق من أن routes/employees.py يحفظ None
grep -n "form\.national_id\.data = None" app/routes/employees.py
```

### مشكلة: الحقول المطلوبة لا تعمل
**الحل:**
```bash
# تحقق من التحقق اليدوي من المتطلبات
grep -n "has_required_errors" app/routes/employees.py
```

---

## ✨ النتائج المتوقعة

### ✅ يجب أن تعمل:
- إضافة موظف بدون رقم قومي
- إضافة موظف برقم قومي خاطئ (مع تحذير)
- إضافة موظف برقم موبايل خاطئ (مع تحذير)
- تحديث بيانات الموظف بنفس الطريقة
- عرض التحذيرات بشكل واضح

### ❌ يجب أن لا تعمل:
- إضافة موظف بدون اسم
- إضافة موظف بدون كود
- إضافة موظف بدون تاريخ التعيين
- حفظ بيانات خاطئة في قاعدة البيانات

---

## 📝 ملاحظات مهمة

1. **الرسائل متعددة الأسطر:**
   - يجب أن تظهر بشكل صحيح في واجهة الويب
   - كل تحذير على سطر منفصل

2. **التحقق من الصيغة:**
   - الرقم القومي: يجب أن يكون 14 رقم بالضبط
   - الموبايل: يجب أن يكون 10-11 رقم

3. **حفظ البيانات:**
   - البيانات الخاطئة لا تُحفظ (يتم حفظ None)
   - البيانات الأخرى تُحفظ بشكل طبيعي

---

## 🚀 الخطوات التالية

بعد التحقق من نجاح جميع الاختبارات:

1. ✅ اختبر إضافة موظفين جدد
2. ✅ اختبر تحديث بيانات الموظفين
3. ✅ تحقق من البيانات في قاعدة البيانات
4. ✅ تأكد من عرض التحذيرات بشكل صحيح

---

## 📞 الدعم

إذا واجهت أي مشاكل:
- تحقق من `EMPLOYEE_FORM_UPDATE.md` للتفاصيل الكاملة
- تشغيل `test_employee_form.py` للتحقق من المنطق
- افحص `app/forms.py` و `app/routes/employees.py` للكود

---

**تم التحديث:** 8 ديسمبر 2024
**الإصدار:** 1.0
